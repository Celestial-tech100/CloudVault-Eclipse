version: "3.9"

services:
  cloudvault:
    build: .
    container_name: cloudvault-eclipse

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - ./database:/app/database

    restart: unless-stopped
